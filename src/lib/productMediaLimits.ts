/** Máximo de imágenes en galería (`image_urls`); la primera es portada (`image_url`). */
export const MAX_PRODUCT_GALLERY_IMAGES = 5;

/** Una publicación puede tener como máximo un video. */
export const MAX_PRODUCT_VIDEOS = 1;
